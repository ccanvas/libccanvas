# ccanvas-config-derive

Macro implementation of `#[derive(CcanvasConfig)]`, implements the [`CcanvasConfig`](https://github.com/ccanvas/libccanvas/blob/master/src/features/config.rs) trait.

Used in [`libccanvas`](https://github.com/ccanvas/libccanvas) feature `config`.
